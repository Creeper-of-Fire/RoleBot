# requirements.txt

# 核心 Discord 库
discord.py

# discord.py 的一个重要依赖，通常会自动安装，但明确列出是好习惯
aiohttp

aiofiles

pytz
python-dotenv
redis[asyncio]>=8.0,<9  # 锁 8.x；max_connections 已在 DataManager 显式设置，仍锁版本防未来 breaking
alembic
SQLAlchemy
pydantic>=2.0
emoji

# 系统监测
psutil
distro